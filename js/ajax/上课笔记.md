# 260527

js 就可以做全栈

DNS解析 ip地址→地址

TCP 协议建立两台电脑之间的连接





## 前后端分离

asynchronous javascript and XML(类似HTML)

简称AJAX 异步js、这个和XML技术是前后端沟通的一种重要机制

后端接口：easy mock。后端写完所有东西，写一个接口。前端调用并显示这个接口，从ip地址请求数据，用作显示。

html标签分三类，块级标签（独占一整行）、行内（没办法设置宽高），行内块（可以设置宽高）

ul是块级，默认有外边距margin（上下外边距）、内边距padding（若为0，则会出现在左上角。如果希望文字不要紧紧挨着容器，就设置内边距）

开标签、闭标签<>、</>